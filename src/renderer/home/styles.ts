/** The mono uppercase section label of the 1c mockups (« DÉJÀ OUVERTS », « AGENTS DÉTECTÉS »). */
export const LABEL = 'm-0 font-mono text-[11px] font-medium tracking-[0.06em] text-dim uppercase';
/** Paths, branches and counts under a name. */
export const META = 'm-0 font-mono text-[12px] text-dim';
