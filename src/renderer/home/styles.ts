/** The mono uppercase section label of the 1c mockups (« DÉJÀ OUVERTS », « AGENTS DÉTECTÉS »). */
export const LABEL =
  'm-0 font-mono text-[10px] font-semibold tracking-[0.08em] text-muted-foreground uppercase';
/** Paths, branches and counts under a name. */
export const META = 'm-0 font-mono text-[11px] text-muted-foreground';
